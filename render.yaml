services:
  - type: web
    name: chronic-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn chronic_agent.api_server:app --host 0.0.0.0 --port 10000"
    pythonVersion: 3.10
    disks:
      - name: data
        mountPath: /data
